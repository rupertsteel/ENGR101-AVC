#include "libE101.h"

float IRsensor() {
 //read IR sensors
 front = ReadAnalog (0);
 right= ReadAnalog (1);
 left = ReadAnalog (2);
float signal =0;
 //if  right reading is more than the left reading we are gonna turn right
 (right>left){
   
 }
 if(left>right){
}if
